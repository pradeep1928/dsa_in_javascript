
/* The code is creating a 3x3 matrix (a 2-dimensional array) with values 0 and 1. The matrix represents
a graph where each element represents a node and the value 1 represents an edge between two nodes. */
const matrix = [
    [0, 1, 0],
    [1, 0, 1], 
    [0, 1, 0]
]

console.log('is matrix edges exits? ', matrix[0][1]);